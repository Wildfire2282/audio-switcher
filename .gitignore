# Rust
/target/
*.rs.bk

# Build outputs (single-file delivery: the exe ships via Releases, never git)
# dist/ holds scripts/package.ps1 artifacts: audio-switcher-v<version>-x64.exe
/dist/
*.exe
*.pdb

# Agent / local cache
/.opencode/
/.tmp/

# Project-scoped agent skills: `skills/<name>/SKILL.md`, tracked so every
# checkout carries the same rules (see .agents/README.md)

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
.DS_Store
Desktop.ini

# Env
.env
.env.*
!.env.example

# Local reference checklists (not part of repo)
references/
